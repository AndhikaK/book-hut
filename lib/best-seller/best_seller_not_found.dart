import 'package:flutter/material.dart';

class BestSellerNotFound extends StatelessWidget {
  const BestSellerNotFound({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Not found"),
      ),
    );
  }
}
